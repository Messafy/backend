services:
  mongodb:
    image: mongo:7
    environment:
      MONGO_INITDB_DATABASE: privatenotes
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
    ports:
      - "27017:27017"
